{
  "name": "@daringway/logger",
  "version": "1.0.0-rc2",
  "license": "MIT",
  "description": "A high-performance, drop-in replacement for console.log() that outputs structured JSON logs and metrics to STDOUT. Built for Node.js, it delivers blazing-fast, non-blocking logging with optional request context, log levels, and ExpressJS/Hono/SolidStart integration—making it ideal for production apps that need speed, consistency, and easy observability.",
  "exports": "./mod.ts",
  "tasks": {
    "test": "deno -A test/logTest.ts",
    "validate": {
      "description": "Check formating and lint",
      "command": "deno $DENO_RUN_ARGS fmt --check && deno $DENO_RUN_ARGS lint  && deno $DENO_RUN_ARGS  check ./"
    }
  },
  "imports": {
    "express": "npm:@types/express@^4.17.21",
    "zod": "npm:zod@^4.0.0"
  },
  "publish": {
    "include": [
      "README.md",
      "./mod.ts",
      "./src/**/*.ts"
    ]
  },
  "lint": {
    "rules": {
      "exclude": []
    }
  }
}
